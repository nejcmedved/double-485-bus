# Example Configuration for Moxa NPort
# Copy this file to config.env and adjust the values

# Moxa NPort Configuration for Port 1 (Solar Inverter)
# This should point to your first Moxa NPort device
NPORT1_HOST=192.168.1.100
NPORT1_PORT=4001

# Moxa NPort Configuration for Port 2 (External System)
# This should point to your second Moxa NPort device
NPORT2_HOST=192.168.1.101
NPORT2_PORT=4002

# TCP Server Configuration
# The bridge will listen on this address/port for external TCP connections
LISTEN_HOST=0.0.0.0
LISTEN_PORT=5020

# Modbus Configuration
# Timeout in seconds for waiting for Modbus responses
MODBUS_TIMEOUT=3
